<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>คำนวณราคาโรบัค</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #000000 0%, #1a1a1a 50%, #0d0d0d 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .container {
            background: linear-gradient(145deg, #0a0a0a 0%, #1a1a1a 100%);
            border-radius: 25px;
            padding: 50px;
            box-shadow: 0 25px 70px rgba(0, 0, 0, 0.8), inset 0 1px 0 rgba(255, 255, 255, 0.05);
            max-width: 500px;
            width: 100%;
            border: 1px solid #252525;
            position: relative;
            overflow: hidden;
        }

        .container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 1px;
            background: linear-gradient(90deg, transparent, #333, transparent);
        }

        h1 {
            color: #ffffff;
            text-align: center;
            margin-bottom: 20px;
            font-size: 32px;
            font-weight: 600;
            letter-spacing: 1px;
            white-space: nowrap;
        }

        .example-box {
            background: linear-gradient(145deg, #000000 0%, #0d0d0d 100%);
            border: 1px solid #1a1a1a;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 35px;
            box-shadow: inset 0 2px 10px rgba(0, 0, 0, 0.5);
        }

        .example-title {
            color: #666666;
            font-size: 13px;
            margin-bottom: 10px;
            letter-spacing: 1px;
            text-transform: uppercase;
        }

        .example-text {
            color: #999999;
            font-size: 15px;
            line-height: 1.6;
        }

        .example-text strong {
            color: #ffffff;
        }

        .input-group {
            margin-bottom: 25px;
        }

        label {
            display: block;
            color: #999999;
            margin-bottom: 10px;
            font-size: 15px;
            font-weight: 500;
            letter-spacing: 0.5px;
        }

        input {
            width: 100%;
            padding: 18px 20px;
            background: #000000;
            border: 2px solid #1a1a1a;
            border-radius: 12px;
            color: #ffffff;
            font-size: 17px;
            transition: all 0.3s ease;
        }

        input:focus {
            outline: none;
            border-color: #333333;
            background: #0a0a0a;
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.05);
        }

        button {
            width: 100%;
            padding: 18px;
            background: linear-gradient(135deg, #1a1a1a 0%, #0d0d0d 100%);
            border: 2px solid #2a2a2a;
            border-radius: 12px;
            color: #ffffff;
            font-size: 18px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 10px;
            letter-spacing: 1px;
        }

        button:hover {
            background: linear-gradient(135deg, #2a2a2a 0%, #1a1a1a 100%);
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.5);
            border-color: #333333;
        }

        button:active {
            transform: translateY(0);
        }

        .result {
            margin-top: 35px;
            padding: 30px;
            background: linear-gradient(145deg, #000000 0%, #0d0d0d 100%);
            border-radius: 18px;
            border: 2px solid #1a1a1a;
            display: none;
            animation: fadeIn 0.5s ease;
            box-shadow: inset 0 2px 10px rgba(0, 0, 0, 0.5);
        }

        .result.show {
            display: block;
        }

        .result-label {
            color: #666666;
            font-size: 14px;
            margin-bottom: 12px;
            letter-spacing: 1px;
            text-transform: uppercase;
        }

        .result-content {
            display: flex;
            align-items: baseline;
            gap: 8px;
        }

        .result-value {
            color: #ffffff;
            font-size: 42px;
            font-weight: 700;
            letter-spacing: 1px;
        }

        .result-unit {
            color: #666666;
            font-size: 20px;
            font-weight: 500;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>คำนวณราคาโรบัค</h1>
        
        <div class="example-box">
            <div class="example-title">ตัวอย่างการคำนวณ</div>
            <div class="example-text">
                โรบัค <strong>1,000</strong> ÷ เรท <strong>8</strong> = <strong>125</strong> บาท
            </div>
        </div>
        
        <div class="input-group">
            <label for="robux">จำนวนโรบัค (Robux)</label>
            <input type="number" id="robux" placeholder="เช่น 1000" min="0">
        </div>

        <div class="input-group">
            <label for="rate">เรทของร้าน (Rate)</label>
            <input type="number" id="rate" placeholder="เช่น 8" min="0.01" step="0.01">
        </div>

        <button onclick="calculate()">คำนวณราคา</button>

        <div class="result" id="result">
            <div class="result-label">ราคาที่ต้องจ่าย</div>
            <div class="result-content">
                <span class="result-value" id="price">0</span>
                <span class="result-unit">บาท</span>
            </div>
        </div>
    </div>

    <script>
        function calculate() {
            const robux = parseFloat(document.getElementById('robux').value);
            const rate = parseFloat(document.getElementById('rate').value);
            const resultDiv = document.getElementById('result');
            const priceSpan = document.getElementById('price');

            if (!robux || !rate) {
                alert('กรุณากรอกข้อมูลให้ครบถ้วน');
                return;
            }

            if (robux <= 0 || rate <= 0) {
                alert('กรุณากรอกตัวเลขที่มากกว่า 0');
                return;
            }

            const price = robux / rate;
            priceSpan.textContent = price.toFixed(2);
            resultDiv.classList.add('show');
        }

        // กด Enter เพื่อคำนวณ
        document.getElementById('robux').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') calculate();
        });

        document.getElementById('rate').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') calculate();
        });
    </script>
</body>
</html>